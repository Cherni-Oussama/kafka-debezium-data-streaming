
{
  "name": "debezium-postgres-connector-with-avro-config",
  "config": {
    "connector.class": "DBZ_CONNECTOR_CLASS",
    "database.hostname": "DBZ_HOST",
    "database.port": "DBZ_PORT",
    "database.user": "DBZ_USER",
    "database.password": "DBZ_PASSWORD",
    "database.dbname" : "DBZ_NAME",
    "topic.prefix": "TOPIC_PREFIX"
  }
}